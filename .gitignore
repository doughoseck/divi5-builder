# NEVER commit credentials
.web-creds.txt
.web-creds*.txt
*.web-creds*
!.web-creds.example.txt

# Local page snapshots / dumps (may contain private site content)
snapshots/
*.raw.html
*.rendered.html

# Node / OS / editor
node_modules/
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# Scratch / working files
scratch/
tmp/
*.tmp

# Module catalogue: generated locally from YOUR copy of Divi (GPL-derived), never committed
catalog/

# Hook profiler output: holds a secret key, generated per use, never committed
d5b-profiler.php
d5b-profiler.key
*.key
